class Genom

    def initialize
        parseFasta.last.delete("\n")
    end

    def parseFasta
        File.read(ARGV[ARGV.length - 1]).split("\n", 2)
    end

    def printFastaSequences 
        puts genom
    end
end
